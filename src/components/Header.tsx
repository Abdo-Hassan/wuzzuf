import React from 'react';

const style = {
  title: {
    fontSize: 20,
  },
};

const Header = () => {
  return (
    <div>
      <p style={style.title}>Header</p>
    </div>
  );
};

export default Header;
